package com.leetcode.vadim.problem.random;

import java.util.logging.LogManager;
import java.util.logging.Logger;

public class Test {
    /*static final Logger LOGGER = LogManager.getLogger(TextReaderTest.class);

    static final String INPUT = "input/input.txt";
    Symbols mainText = new Text();

    @Before
    public void justRead(){
        String readingStr = new TextReader().readToOneString(INPUT);
        TextParser tp = TextParser.getInstance(readingStr);
        tp.parse(mainText, "");
        mainText.print();
    }

    @Test
    public void readerTest1(){
        String str = mainText.toString();
        Assert.assertEquals("sed",str.substring(147,150));
    }
    @Test
    public void readerTest2(){
        String str = mainText.toString();
        Assert.assertEquals(" a",str.substring(50,52));
    }
    @Test
    public void readerTest3(){
        String str = mainText.toString();
        Assert.assertEquals("n",str.substring(26,27));
    }
    @Test
    public void readerTest4(){
        String str = mainText.toString();
        Assert.assertEquals("ain",str.substring(95,98));
    }*/
}
